# TODO

- Revisit face recognition on Python 3.14 and replace the current `dlib`-based stack with a more maintainable alternative.
- Decide whether face recognition should remain an optional extra or move to a separate integration module.
- Validate a replacement against current training data, recognition accuracy, and runtime performance.
- Update `README.md`, `AGENTS.md`, and configuration examples once the replacement path is chosen.
